{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "build:patient-apk": "pnpm --filter @workspace/patient-app run apk",
    "build:doctor-apk": "pnpm --filter @workspace/doctor-app run apk",
    "build:patient-aab": "pnpm --filter @workspace/patient-app run aab",
    "build:doctor-aab": "pnpm --filter @workspace/doctor-app run aab"
  },
  "private": true,
  "devDependencies": {
    "feather-icons": "^4.29.2",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
